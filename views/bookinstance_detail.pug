extends layout

block content

  h1 ID: #{bookinstance._id}
  
  p #[strong Título:] 
    a(href=bookinstance.book.url) #{bookinstance.book.title}
  p #[strong Impressão:] #{bookinstance.imprint}

  p #[strong Status:] 
    if bookinstance.status=='Available'
      span.text-success #{bookinstance.status}
    else if bookinstance.status=='Maintenance'
      span.text-danger #{bookinstance.status}
    else
      span.text-warning #{bookinstance.status} 
      
  if bookinstance.status!='Available'
    p #[strong devida de volta:] #{bookinstance.due_back_formatted}

  hr
  p
    a(href=bookinstance.url+'/delete') Excluir a instância do livro
  p
    a(href=bookinstance.url+'/update') Atualizar Instância do Livro